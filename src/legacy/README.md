# Legacy pipeline code

Các file trong thư mục này là implementation trước khi hợp nhất PoC. Chúng chỉ được giữ để đối chiếu lịch sử và không thuộc runtime chính.

Runtime canonical nằm trong `src/pipeline/`; test canonical nằm trong `tests/`.
